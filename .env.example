# ============================================
# DATABASE CONFIGURATION
# ============================================

# Local PostgreSQL (Docker or system-installed)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/samoa_finance"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/samoa_finance"

# ============================================
# DOCKER CONFIGURATION (for docker-compose)
# ============================================
# PostgreSQL container settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=samoa_finance

# ============================================
# NEXTAUTH CONFIGURATION
# ============================================
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here
