# ============================================
# DATABASE CONFIGURATION
# ============================================

# Option 1: Local PostgreSQL (Docker or system-installed)
# Uncomment these for local development:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/samoa_finance"
# DIRECT_URL="postgresql://postgres:postgres@localhost:5432/samoa_finance"

# Option 2: Supabase (Cloud PostgreSQL)
# Get these from Supabase Dashboard → Project Settings → Database
# Connection String: Use "Session mode" for DATABASE_URL and "Transaction mode" for DIRECT_URL
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# ============================================
# DOCKER CONFIGURATION (for docker-compose)
# ============================================
# PostgreSQL container settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=samoa_finance

# ============================================
# NEXTAUTH CONFIGURATION
# ============================================
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here
